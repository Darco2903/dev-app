export type LocaleTL = {
    system: string;
    en: string;
    fr: string;
};

export type ThemeTL = {
    system: string;
    light: string;
    dark: string;
};

export type RefreshTL = {
    refresh: string;
};
